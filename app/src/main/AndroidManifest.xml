<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

	<!--
		REGULA DE AUR (SPEC §9): aplicația funcționează 100% offline.
		Nu se adaugă niciodată permisiuni de rețea: INTERNET, ACCESS_NETWORK_STATE,
		ACCESS_WIFI_STATE. Dacă o funcție are nevoie de rețea, nu intră în aplicație.
		Numele complete ale permisiunilor sunt scrise intenționat doar în
		tools/check-no-internet.sh, ca verificarea să nu se agațe de comentarii.
	-->

	<application
		android:name=".App"
		android:allowBackup="false"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/Theme.Lucrari">

		<activity
			android:name=".MainActivity"
			android:exported="true"
			android:label="@string/app_name"
			android:theme="@style/Theme.Lucrari"
			android:windowSoftInputMode="adjustResize">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
	</application>

</manifest>
